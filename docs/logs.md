### 2026-06-10 21:43:18 CST - 工程初始化

目标：根据 `docs/todo.md` 搭建 React + TypeScript 单页工具工程。

实现方式：创建 Vite 工程配置、TypeScript 配置、Vitest 测试环境、入口 HTML 和 React 挂载入口；引入 Mantine、Tabler Icons、Three.js、Vitest 与 Testing Library，并生成 `package-lock.json`。

### 2026-06-10 21:43:18 CST - 全景预览与镜头功能

目标：提供黑色网点背景、顶部工具栏、16:9 全景预览区、镜头选择与视角控制能力。

实现方式：使用 Three.js 创建球面内侧贴图预览，将附带的 `assets/prono-0610-1534.png` 作为默认全景图；镜头焦段通过预设 FOV 映射实现，切换镜头时保留当前 yaw、pitch 与 dolly；控制按钮通过纯函数更新虚拟摄像机的水平角、俯仰角和前后偏移。

### 2026-06-10 21:43:18 CST - 图片选择与拍照保存

目标：支持本地图片替换与按当前镜头视窗截图保存。

实现方式：使用 Mantine `FileButton` 读取本地图片并以 Object URL 替换当前贴图；Three.js renderer 开启 `preserveDrawingBuffer`，拍照时从 canvas 生成 PNG Blob，并按 `prono-shot-{camera}-{degree}-{datetime}.png` 格式触发本地下载。

### 2026-06-10 21:43:18 CST - 测试与验证

目标：完成镜头选择和镜头控制能力的单元/UI 验证。

实现方式：新增镜头预设、文件名生成、角度归一化、俯仰限制、控制动作等单元测试；新增工具栏 UI 测试，覆盖焦段切换、镜头控制按钮和拍照按钮事件。已执行 `npm test`，结果为 3 个测试文件、10 个测试全部通过；已执行 `npm run build`，TypeScript 和生产构建均通过。

### 2026-06-10 21:43:18 CST - 浏览器验收

目标：使用附带图片进行实际页面验证。

实现方式：启动本地开发服务并在浏览器打开 `http://localhost:5173/`；确认工具栏、默认全景图、16:9 预览区渲染正常；点击 `50mm` 后视野收窄，点击 `左10` 与 `上仰20` 后状态更新为 `Yaw -10 deg / Pitch 20 deg / Offset 0.0`。Codex 内置浏览器不支持下载事件捕获，因此下载动作未在该浏览器中完成自动化断言；普通浏览器会使用原生 `<a download>` 保存截图。

### 2026-06-10 22:17:35 CST - 镜像与鼠标交互修复

目标：处理全景画面左右镜像、视窗缺少鼠标交互、前进/后退没有可见效果的问题。

实现方式：在 Toolbar 增加 `镜像` 按钮，通过切换全景球体 X 轴缩放实现水平镜像开关；在全景视窗增加左键拖拽事件，水平拖拽映射到 yaw，垂直拖拽映射到 pitch，并保留俯仰角限制；滚轮映射到 dolly 缩放。由于全景球面贴图没有真实空间视差，单纯移动相机位置不会产生明显变化，因此将 `前进` / `后退` 调整为基于当前焦段 FOV 的有效视角缩放，使工具栏按钮与鼠标滚轮都能产生可见的前后镜头效果。

验证方式：执行 `npm test`，结果为 3 个测试文件、12 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；浏览器验证中，`前进` 后状态由 `Zoom 0` 变为 `Zoom +20`，`后退` 回到 `Zoom 0`，拖拽后状态更新为 `Yaw 7 deg / Pitch 4 deg`，滚轮后状态更新为 `Zoom +25`，镜像按钮可切换为选中状态且画面保持正常渲染。

### 2026-06-10 22:27:51 CST - 拖拽方向与缩放比例调整

目标：让上下拖拽方向更符合当前使用习惯，并提高 35mm、50mm 等焦段下的继续缩放空间。

实现方式：将视窗垂直拖拽映射反向，向下拖拽时 pitch 增加，向上拖拽时 pitch 减少；将 dolly 最大值从 `1.8` 提高到 `3.5`，并将最小有效 FOV 从 `6` 度降低到 `2` 度，使长焦镜头仍能继续放大。

验证方式：执行 `npm test`，结果为 3 个测试文件、13 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；浏览器验证中，向下拖拽后状态由 `Pitch 0 deg` 变为 `Pitch 7 deg`，连续滚轮后状态可达到 `Zoom +350`。

### 2026-06-10 22:46:55 CST - 去畸变开关与镜头标签压缩

目标：在 50mm 及以上焦段或明显缩放时提供去畸变能力，并减少镜头按钮占用空间。

实现方式：新增 `去畸变` Switch，默认开启；在开启状态下，当焦段大于等于 50 或当前 zoom 超过阈值时，使用基于 equirectangular 纹理采样的 rectilinear shader 进行校正渲染，其余场景继续使用原有球面预览。将镜头选择按钮标签从 `10mm/25mm/...` 改为 `10/25/...`，截图文件名仍保留原焦段 id。

验证方式：执行 `npm test`，结果为 3 个测试文件、15 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；浏览器验证中，`去畸变` 开关默认选中，镜头标签显示为纯数字，50 焦段与 zoom 场景下画面正常渲染。

### 2026-06-10 22:58:28 CST - 去畸变渲染方向与颜色修复

目标：修复 50mm 去畸变模式下画面上下颠倒、颜色异常变深的问题。

实现方式：在 rectilinear shader 中反转屏幕 Y 轴采样方向，补充 Three.js 的 `colorspace_fragment` 输出转换，让自定义 shader 与内置材质走一致的 sRGB 输出流程；同时调整水平经度基准，使去畸变开关打开/关闭时取景中心保持一致。

验证方式：执行 `npm test`，结果为 3 个测试文件、15 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；浏览器对照验证中，50 焦段下去畸变开/关均未出现上下颠倒，颜色亮度保持一致，取景中心无明显跳变。

### 2026-06-10 23:13:16 CST - 去畸变投影差异增强

目标：解决去畸变开关打开/关闭视觉效果几乎完全一致的问题。

实现方式：上一版去畸变使用 rectilinear 采样，而原始 Three.js 透视相机查看全景球本身也是 rectilinear 投影，所以两者数学上接近等价。本次将去畸变路径改为以当前相机朝向为中心的局部经纬平面采样，降低边缘拉伸感；关闭开关时仍保留原始真实透视取景。

验证方式：执行 `npm test`，结果为 3 个测试文件、15 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；浏览器对照验证中，50 焦段并 zoom 到 `+100` 后，去畸变开/关在边缘比例和局部形状上已可见差异，画面未出现上下颠倒或颜色变暗。

### 2026-06-10 23:27:56 CST - 去畸变可见效果修正

目标：修正 50 焦段、`Zoom -100` 场景下去畸变开关仍然几乎没有可见差异的问题。

实现方式：确认上一版局部经纬采样在该场景下差异仍不足，改为在去畸变 shader 中加入随焦段、后退缩放和有效 FOV 增强的边缘反拉伸强度；50mm 及以上焦段开启去畸变时，画面边缘区域会被主动压回，降低原始透视取景在边缘和角落的拉伸感。关闭开关时继续使用原始球面透视预览。

验证方式：执行 `npm test -- --run`，结果为 3 个测试文件、15 个测试全部通过；执行 `npm run build`，TypeScript 与生产构建均通过；在浏览器打开用户正在使用的 `http://localhost:5174/`，选择 `50` 并滚轮后退到 `Zoom -100`，只切换 `去畸变` 开关，确认打开和关闭时边缘比例与局部形状出现明显差异，同时没有上下颠倒和颜色变暗。
