# 深度辅助后退实验

## 分支目标

分支：`feature/depth-assisted-dolly`

本分支用于开发 `docs/suggestion.md` 中的方案五：深度辅助的真实前进 / 后退。

当前主线的前进 / 后退基于 FOV 缩放，能模拟视角变化，但不是相机真实位移。该实验分支的目标是先搭建一条真实相机位移渲染链路：开启深度后退后，前进 / 后退不再扩大或缩小 FOV，而是让相机沿当前视线方向移动。

## 当前实现

### 深度后退开关

工具栏新增 `深度后退` 开关。

开启后：

- 当前镜头焦段的基础 FOV 保持不变。
- `pose.dolly` 会转换为相机沿视线方向的真实位移。
- 前进 / 后退控制仍复用原来的 `dolly` 状态。
- 渲染对象从普通球面全景切换为带深度代理的全景网格。

关闭后：

- 回到主线逻辑：通过有效 FOV 模拟前进 / 后退。

### 深度图入口

工具栏新增 `选择深度图` 图标按钮。

如果上传深度图：

- 深度图按 equirectangular UV 对齐读取。
- 灰度值会转换为全景网格的半径变化。
- 相机移动时可以产生比普通全景球更明显的视差。

如果没有上传深度图：

- 使用程序化深度代理。
- 当前代理假设画面中部较远、顶部和底部较近，并加入很轻的水平变化。
- 这个代理只用于验证渲染链路，不代表真实空间深度。

## 已知限制

1. 当前没有接入真实深度估计模型。
2. 程序化深度代理只能产生有限视差，不能准确还原室内物体关系。
3. 上传深度图需要用户自己保证与全景图对齐。
4. 相机移动后，原图没有拍到的遮挡区域仍然无法真实补全。
5. 如果深度图质量差，视差可能比 FOV 缩放更不自然。

## 后续方向

建议下一步按以下顺序推进：

1. 增加深度图预览或状态提示，让用户知道当前使用的是“深度图”还是“代理深度”。
2. 提供深度强度滑条，用于控制网格位移幅度。
3. 研究浏览器端或服务端深度估计模型，生成与全景图对齐的深度图。
4. 在相机移动后增加边缘 / 空洞处理策略。
5. 将实验模式和主线 FOV 缩放模式清晰区分，避免用户误以为它们是同一种“后退”。

## 验证记录

- `npm test -- --run`：3 个测试文件、17 个测试通过。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- 本地服务 `http://localhost:5175/` 使用 `curl` 返回 200。
- Codex in-app browser 打开本地端口时返回 `ERR_BLOCKED_BY_CLIENT`，因此本轮未完成浏览器视觉截图验证。
